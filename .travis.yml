language: java

jdk:
  - openjdk8

branches:
  only:
  - development

before_script: 
  - ls -la # this command just prints the contents of the current working directory.

script: 
  - mvn clean package jacoco:report
  - java –jar Design_pattern-0.0.1-SNAPSHOT-jar-with-dependencies.jar ../src/test/resources/TestClass.java regex local  ../output_metrics_file csv

after_success:
- bash <(curl -s https://codecov.io/bash) -t 1036eb64-2d96-44c6-b132-0dde4e7dfcca
